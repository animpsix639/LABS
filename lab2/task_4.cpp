#include <iostream>
#include "func.h"
#define SUM(a, b) ((a) + (b)) 
namespace yo = std;

int main()
{
    yo::cout << "Результат суммы:" << SUM(4, 6) << yo::endl;
    spacespace::func(5);
    spacespace::func(10);
    spacespace::func(3);
    return 0;
}